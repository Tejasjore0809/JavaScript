//Javascript program to print the number of an element present in array.

function printNumberOfElements(arr) {
  console.log(`Number of elements in an array:`, arr.length);
}

let array = [1, 2, 3, 4, 5, 6, 7];
printNumberOfElements(array);
